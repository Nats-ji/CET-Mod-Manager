-- Save/restore cet binding settings for disabled mods

local bindings = {}

-- Save bindings
function bindings.Save(aMod) -- mod: aMod


end

-- Restore bindings
function bindings.Restore(aMod)
  
end


return bindings